# 安全与敏感信息报告

## 支持范围

安全修复以当前 `main` 分支和最新正式发布版本为准。旧版本可能不会单独回补修复；报告问题前请先确认最新版仍可复现。

## 不要公开提交敏感信息

请勿在 GitHub Issue、讨论区、Pull Request、截图、录屏或诊断包中提交以下内容：

- SOCKS5 地址、端口、用户名或密码；
- 完整 `vless://` 链接、二维码、UUID、Short ID 或 REALITY 私钥；
- `/etc/xray-chain/`、`/var/lib/xray-chain/` 中的配置、状态或备份；
- 未脱敏的 systemd/Xray 日志、服务器公网 IP、域名、账号或客户数据；
- API Token、SSH 私钥、云厂商凭据及其他可用于访问系统或第三方服务的信息。

如果凭据已经公开，请先在对应服务中吊销或轮换；删除 Issue 或提交不能保证历史副本、通知、Fork、缓存或第三方存档同步消失。

## 如何报告

优先使用仓库 Security 页面中的 Private vulnerability reporting（若已启用）。如果该功能不可用，只能创建不含敏感信息的公开 Issue：说明受影响版本、脱敏后的复现步骤、预期结果和实际结果，并用占位符替代所有地址、标识和凭据。

维护者不需要、也不会要求你提供真实 SOCKS5 密码、完整节点链接、REALITY 私钥或服务器登录凭据。无法在不披露秘密的情况下复现时，请先停止公开提交并等待私密报告渠道。

## 运营者应急处理

- 泄露 SOCKS5 凭据：在上游供应商处立即轮换，再使用 `puppyip edit <节点编号>` 更新。
- 泄露 VLESS 链接或二维码：使用 `puppyip reset <节点编号>` 轮换该节点凭据并停止使用旧链接。
- 泄露配置、备份、服务器登录信息或 REALITY 私钥：按已泄露处理，轮换所有相关凭据并检查 VPS、上游账号和访问日志。

不要仅依赖删除本地文件或 Git 提交来完成处置；已经推送到公开仓库的信息可能仍存在于 Fork、缓存或历史对象中。
